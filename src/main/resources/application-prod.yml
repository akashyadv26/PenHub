
server:
  port: 8083
  servlet:
    context-path: /api/${spring.application.name}


spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/PenHub


  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect=org: org.hibernate.dialect.MySQLDialect

